name: Deploy GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: app/kotenseki_annotation/package-lock.json

      - name: Install dependencies
        working-directory: app/kotenseki_annotation
        run: npm ci

      - name: Build React app
        working-directory: app/kotenseki_annotation
        run: npm run build

      - name: Assemble site
        run: |
          mkdir -p _site/app/kotenseki_annotation

          # 既存のルート静的サイトをコピー
          rsync -av \
            --exclude '.git' \
            --exclude '.github' \
            --exclude 'app/kotenseki_annotation' \
            ./ _site/

          # Reactアプリのbuild結果を /app/kotenseki_annotation/ に配置
          rsync -av app/kotenseki_annotation/dist/ _site/app/kotenseki_annotation/

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site

      - name: Show assembled files
        run: |
          find _site -maxdepth 5 | sort

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

